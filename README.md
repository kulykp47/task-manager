## 📝 Менеджер Задач (Task Manager)

Это простое настольное приложение для управления задачами, созданное с использованием библиотеки **Tkinter** в Python.

---

### ✨ Особенности

* **Добавление задач:** Добавляйте новые задачи, указывая название, описание и срок выполнения.
* **Удаление задач:** Удаляйте существующие задачи из списка.
* **Постоянное хранение:** Задачи автоматически сохраняются в файл `tasks.json` и загружаются при запуске приложения.
* **Экспорт в TXT:** Сохранение текущего списка задач в текстовый файл `tasks.txt`.
* **Простой интерфейс:** Интуитивно понятный графический интерфейс.

---

### 🚀 Запуск проекта

Для запуска проекта вам понадобится установленный **Python 3**.

1.  **Клонируйте репозиторий** (если применимо) или сохраните предоставленные файлы (`main.py`, `gui.py`, `classes.py`) в одной директории.
2.  **Запустите главный файл** `main.py` из командной строки:

    ```bash
    python main.py
    ```

---

### 📂 Структура проекта

Проект состоит из трех основных файлов:

* **`main.py`**: Точка входа в приложение. Инициализирует главное окно Tkinter и запускает цикл обработки событий.
    ```python
    from gui import *

    if __name__ == "__main__":
        root = tk.Tk()
        app = TaskManagerApp(root)
        root.mainloop()
    ```

* **`classes.py`**: Определяет классы данных и логики.
    * `Task`: Класс для хранения данных одной задачи (название, описание, срок выполнения).
    * `TaskManager`: Класс для управления списком задач (добавление, удаление, сохранение/загрузка в `tasks.json`, экспорт в `tasks.txt`).

* **`gui.py`**: Определяет класс графического интерфейса.
    * `TaskManagerApp`: Класс, который настраивает пользовательский интерфейс (поля ввода, кнопки, список) и связывает действия пользователя с методами `TaskManager`.

---

### 🛠 Использование

1.  **Добавление задачи:**
    * Введите **Название** задачи.
    * Введите **Описание** задачи.
    * Введите **Срок выполнения** в формате **ГГГГ-ММ-ДД**.
    * Нажмите кнопку **"Добавить"**.
2.  **Удаление задачи:**
    * Выберите задачу в списке, кликнув по ней.
    * Нажмите кнопку **"Удалить"**.
3.  **Сохранение в TXT:**
    * Нажмите кнопку **"Сохранить"** для экспорта текущего списка задач в файл
